# Chrono24 Scraper

Backend Node.js + Playwright pour scraper Chrono24.

## Déploiement Render

- Build Command: `npm install && npx playwright install --with-deps chromium`
- Start Command: `npm start`
